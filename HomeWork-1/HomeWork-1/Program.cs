using System;

namespace C_Training_Test_01
{
    class Program
    {
        static void Main(string[] args)
        {

            Console.WriteLine("Max and min values of byte are {0} -- {1}", byte.MaxValue, byte.MinValue);
            Console.WriteLine("Max and min values of short are {0} -- {1}", short.MaxValue, short.MinValue);
            Console.WriteLine("Max and min values of int are {0} -- {1}", int.MaxValue, int.MinValue);
            Console.WriteLine("Max and min values of long are {0} -- {1}", long.MaxValue, long.MinValue);
            Console.WriteLine("Max and min values of float are {0} -- {1}", float.MaxValue, float.MinValue);
            Console.WriteLine("Max and min values of double are {0} -- {1}", double.MaxValue, double.MinValue);
            Console.WriteLine("Max and min values of uint are {0} -- {1}", int.MaxValue, int.MinValue);
            Console.WriteLine("Max and min values of ulong are {0} -- {1}", long.MaxValue, long.MinValue);

            // [10.02.2022 13:08]
            //Задача- вывести в консоль максимальные и минимальные допустимые значения для типов данных byte, short, int, long,  float, double, uint, ulong.

        }
    }
}
